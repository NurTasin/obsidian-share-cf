{
  "name": "obsidian-share-cf-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "db:migrate:local": "wrangler d1 migrations apply obsidian-share-cf --local",
    "db:migrate:remote": "wrangler d1 migrations apply obsidian-share-cf --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260423.0",
    "@types/markdown-it": "^14.1.2",
    "typescript": "^5.4.5",
    "wrangler": "^4.14.0"
  },
  "dependencies": {
    "markdown-it": "^14.1.1"
  }
}
